image: Visual Studio 2019

environment:
  matrix:
      - SOLUTION: HW1/BWTAlgorithm/BWTAlgorithm.sln
      - SOLUTION: HW1/SortingArray/SortingArray.sln

before_build:
    - For /R %%I in (*.sln) do nuget restore %%I
    
build_script:
    - msbuild %SOLUTION%
    
test_script:
    - dotnet test %SOLUTION%
